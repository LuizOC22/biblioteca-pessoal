spring.application.name=biblioteca-pessoal

# --- BANCO DE DADOS (Híbrido: Nuvem ou Local) ---
# A sintaxe é: ${NOME_DA_VARIAVEL_NA_NUVEM : valor_padrao_local}

spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/biblioteca_pessoal}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:Oliver%12}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect